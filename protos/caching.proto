syntax = "proto3";


message CachedQuery {
    string query = 1;
    string data = 2;
}

message Ack {
    int32 response = 1;
}

service CachingService {
    rpc CacheQuery (CachedQuery) returns (Ack);
}